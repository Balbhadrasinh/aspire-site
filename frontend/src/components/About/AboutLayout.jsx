const AboutLayout = ({
  aboutSectionData,

}) => {
  const aboutData = aboutSectionData?.data?.attributes?.aboutPage;

  const heroImage = heroSectionData?.heroImage?.data?.attributes?.url;

  return (
    <>
      <H1>HII</H1>
    </>
  );
};

export default AboutLayout;
